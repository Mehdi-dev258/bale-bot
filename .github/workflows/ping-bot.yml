name: Ping Bale Bot

on:
  schedule:
    - cron: '*/5 * * * *'  # هر ۵ دقیقه اجرا بشه
  workflow_dispatch:        # امکان اجرای دستی

jobs:
  ping:
    runs-on: ubuntu-latest
    
    steps:
    - name: Send request to bot webhook
      run: |
        curl -X GET https://ae4a562f.coinsurf.pages.dev/bot
        echo ""
        echo "✅ Request sent at $(date)"
